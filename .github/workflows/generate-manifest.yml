name: Generate Resource Manifest

on:
  push:
    branches: [ main ]
    paths: [ 'resources/**' ]  # Run when resources (including images) are added/changed
  workflow_dispatch:  # Allow manual trigger

jobs:
  generate-manifest:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.x'
    
    - name: Generate resource manifest
      run: python resources/scripts/generate_resource_manifest.py
    
    - name: Check for changes
      id: verify-changed-files
      run: |
        if [ -n "$(git status --porcelain)" ]; then
          echo "changed=true" >> $GITHUB_OUTPUT
        else
          echo "changed=false" >> $GITHUB_OUTPUT
        fi
    
    - name: Commit and push changes
      if: steps.verify-changed-files.outputs.changed == 'true'
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add resources/resource-manifest.json
        git commit -m "Auto-update resource manifest [skip ci]"
        git push